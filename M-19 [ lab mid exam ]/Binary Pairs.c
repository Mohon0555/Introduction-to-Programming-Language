#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int t,n,s;
    char bin[n];
   // scanf("%d",&t);

   // sacnf("%d", &n);

    fgetc(bin, sizeof(bin),stdin);
    puts(bin);
    return 0;
}
